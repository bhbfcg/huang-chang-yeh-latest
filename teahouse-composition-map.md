# TEAHOUSE COMPOSITION MAP / VERSION 02

這一版不把 14 張圖片視為 14 個 Gallery Item。先設計沒有圖片時仍然成立的版面，再把既有影像嵌入空間。可見文字只使用目前資料中的 `detail.intro`、`detail.body`、`detail.galleryTitle`、`detail.galleryIntro`、project metadata 與既有 captions。

## 無圖片的版面骨架

| Spread | 沒有圖片時的構圖 | 文字角色 | 空間規則 |
|---|---|---|---|
| Opening | 大片紙色空間、左上微型導覽、右下資料錨點 | TEAHOUSE 作為唯一巨大字級，副標退到邊緣 | 不先放 Grid，不用線條分割 |
| Interval 01 | 只有一個偏右的小型文字島 | 使用 `detail.intro`，讓它像編輯註記而非段落標題 | 左側保留超過半個 viewport 的空白 |
| Composition A | 12 欄 Grid 被切成兩個不對齊的文字區 | `detail.galleryTitle` 與 `detail.galleryIntro` 分開出現 | 一個靠上、一個靠下，不形成卡片 |
| Interval 02 | 大型垂直空白與一個貼近邊緣的編號 | 不顯示章節標題 | 空白本身改變閱讀速度 |
| Composition B | 左側狹長區與右側寬闊區互相錯位 | `detail.body` 只在一個下方文字島出現 | 文字不包在圖片下方，不使用對稱欄 |
| Composition C | 文字退到頁面邊界，中央只留下可穿越的空間 | 只使用既有 caption 作為微型 annotation | Grid 斷裂，讓圖片可後置或缺席 |
| Final interval | 空白加上下一段導覽的微小方向感 | 不新增結尾標語 | 先停留，再進入既有 Previous / Next |

## 既有圖片嵌入規則

`01` 不作為固定 Hero Gallery Item，而作為 Opening 的右側／下緣 Anchor，讓 TEAHOUSE 與影像互相穿插。`02` 與 `03` 分別放入 Composition A 的兩個不等寬位置，中間保留空間，不構成標準雙欄。`04` 單獨穿越 Grid，成為一個節奏斷點。`05` 放入 Composition B 的寬側，與文字島保持距離。`06` 只作為靠邊的細節標記，避免每張圖都取得同等注意力。`07` 與既有 `detail.body` 形成圖文錯位關係，文字不直接貼在圖片下方。`08` 作為 Composition C 的邊界穿越圖。`09` 單獨形成一個大型視覺停留，但不配套新增標題。`10` 只作為小型旁註圖。`11` 與 `12` 不是兩個連續 Gallery Item，而是左右兩個不在同一水平線上的視覺對照。`13` 形成最後一個空間速度轉換。`14` 作為 Final visual climax，在空白之後獨立出現，接近滿版但保留版面邊界。

## Composition 原則

第一，圖片不再決定整個頁面的節奏；空白、文字島、錯位 Grid 與圖片之間的距離先決定節奏。第二，每個 Spread 不必同時有標題、圖片與段落。第三，Section marker 只作為資料定位，不作為主視覺。第四，任何一張圖片被移除時，留下的文字、空白與 Grid 仍應形成可讀的 Editorial Spread，而不是空掉的 Gallery slot。

## 驗收方式

先檢查只顯示紙色背景、TEAHOUSE、既有 intro／body／galleryIntro 與 metadata 時，頁面是否仍有明確的閱讀方向、視覺重心與速度變化。再檢查加入圖片後，圖片是否只是強化 Composition，而不是重新把頁面變回 Image → Image → Image。
